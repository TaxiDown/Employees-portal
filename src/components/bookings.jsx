import { useTranslations } from 'next-intl';
import React from 'react'

export default function Bookings() {
  const dict = useTranslations('lang');
/* bookings table (booking number, status, contact info, isReturn)
  drivers table 
  booking details (//, driver and each ride details, set driver, change status)
  driver information(his rides, name, ....)
 */
  return (
    <div>
      
    </div>
  )
}
